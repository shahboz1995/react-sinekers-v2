const initialState ={
  isLoading: false,
  favorites: [],
}
const sneakersReducer = (state = initialState, action)=>{
  switch (action.type) {
    
    default:
      return state;
  }
}

export default sneakersReducer;